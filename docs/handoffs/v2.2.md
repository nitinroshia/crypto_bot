# Developer Handoff v2.2: ETH/USDT-era research pipeline (as of 2026-09-20, after work order 1.3)

**Version:** v2.2 (work order 1.3 re-issue of v2.1; v2 was work order 1.2).

Read this first. It is written so a fresh developer session can continue without the earlier conversation.

## What changed and why (v1 -> v2.2)

- **Costs.** The planning fee is 0.10% for makers and takers; the account's current maker 0% is a promotion and is ignored. Costs are two cases, base (0.24% round trip) and stress (0.30%), taker execution at the next bar's open (instruction carried in work order 1.2). The Scenario A / A-stress logic, the fill rules and the old materiality thresholds were removed from the decisions in force.
- **Research pair.** ETH/USDT, with discovery and walk-forward on data up to and including 2025-08-31 and a locked holdout after it. ETH/FDUSD is reference only.
- **Retirements** (kept as history, see `user_data/analysis/retired/README_retired.md`): Task 1 (all versions and its gate), Task 5, Task 2 follow-ups, the liquidity monitor, fill-rule reporting, Scenario A, A-stress and the re-costing tool. Only the Scenario A/B cost model existed as code (archived); the rest were specs only. Task 2's own code stays in place as history.
- **Section 7 is now the mathematician's new-phase order, embedded verbatim.** Work order 1.2 itself was housekeeping only: nothing from the new phase was built.
- Everything about ETH/FDUSD results, retired tasks and earlier decisions is labeled history.
- **v2.1 (same day):** work order 1.2 was verified on the owner's machine (all self-tests passed; `tree-02.txt` sizes match the delivered files); the `scripts/backtest.sh` comment was fixed in an addendum (comment only); section 11 listed the questions to the mathematician that were still open (answered in v2.2).
- **v2.2 (work order 1.3):** clarifications from the mathematician folded in (sections 6 and 7.6 onward): forward-test minimum sample restored, economic gate and pre-registered rules for the slope cells, S2 endpoints, cutoff mechanics, partial years, Task 4 definition, registry rule, and the primary-timeframe decision (5m and above; 1m unused). `costs.py` gained `--per-side`; `scripts/backtest.sh` now takes its fee from it (`COST_CASE`, `PRINT_ONLY`, `TIMERANGE` ending 20250831); README and project_context updated to match. The six open questions of v2.1 are answered (section 11).

## 1. Roles and working rules

- **Project owner** ("the bridge") runs every command on their Mac and pastes back outputs. Do not ask them development questions; give exact, copy-pasteable commands with **no placeholders** (a `<timestamp>` placeholder was once typed literally and failed).
- **Mathematician** writes work orders and decides research design and anything touching the financial model. Route those questions to them via a forwardable message.
- **Developer** (you) builds and tests code. Hard rules:
  1. Never assume anything that affects the financial model. Where a spec allows two readings, run **both** and label them.
  2. Every interpretation you adopt is stated in the code docstring and the report.
  3. Test on synthetic data with **known ground truth** and, where a shared tool changes, regress against the previous outputs.
  4. Ask the owner to capture logs with `| tee name.log`. Verify an installed version by file size or `--help`, not by assumption.
  5. The owner expects files to be read in full. Say plainly what you did not see, and never report a file's content from memory.
- Project docs: the repo's `project_context.md`, `README.md`, and the mathematician's messages listed in section 8.

## 2. Environment

- Repo root: `~/apps/crypto_bot`. Analysis code: `user_data/analysis/`. Freqtrade data: `user_data/data/binance/`. Raw-kline store: `user_data/data/binance_raw/` (separate on purpose: freqtrade assigns six column names when it reads a feather file, so files with extra columns must not sit in its data dir).
- Python 3.12 in `.venv`; scripts run as plain Python from the repo root (not inside Docker). Freqtrade runs in Docker (`docker compose run --rm freqtrade ...`).
- Development sandbox has no route to Binance; downloads and real-data runs happen only on the owner's machine.
- Dependencies: pandas>=2.0, numpy>=1.24, scipy>=1.10, statsmodels>=0.15 (0.14 fails in `stats_fingerprint.py`: `adfuller(result_object=...)`), pyarrow>=14. Code was tested on pandas 3.0.2 and on pandas 2.2.3 / numpy 1.26.
- `bash user_data/analysis/selftest_all.sh` runs every offline self-test (14 scripts). Self-tests passed on the owner's machine after work order 1.2 (`terminal-05.txt`: ALL SELF-TESTS PASSED, 14 scripts).

## 3. Installed state

Sizes in bytes. Every file below was installed and verified after work order 1.2 (`tree-02.txt`, `terminal-05.txt`). Work order 1.3 then changed `costs.py` (now with `--per-side`) and `scripts/backtest.sh`; both are **installed and verified on the owner's machine** (`terminal-06.txt`). `README.md`, `project_context.md` and this handoff were re-issued by work order 1.3 (v2 / v2.2).

| File | Bytes | Role |
|---|---|---|
| `leadlag.py` | 38,029 | Lead-lag tools. `event_anchored_lead_lag` with strict time alignment; `grid_returns`; columns `lag_bars`, `lag_minutes`, `correlation` |
| `research_cli.py` | 28,206 | Formula runner and walk-forward. PAIR:tf keys; family size + Bonferroni/Holm; fixed-lag validate; half-open windows |
| `run_fingerprint.py` | 12,447 | Fingerprint battery. `--start-date`; event-anchored family/adjusted p; writes `data_ranges.json` |
| `resample.py` | 13,469 | Resampling; rule "traded" is the default |
| `task0_report.py` | 27,390 | Data inventory, seam check, monthly flat table; reports the new cost model |
| `fetch_klines.py` | 33,638 | Raw klines + exchange filters; `--start`, `--export-freqtrade` |
| `costs.py` | 6,710 | Base / stress taker cost model (work order 1.2); `--per-side {base,stress}` prints 0.0012 / 0.0015 (work order 1.3) |
| `passive_fill_baseline.py` | 26,752 | **History** (Task 2); no longer imports `costs.py` |
| `databundle.py`, `multitest.py` | 7,281 / 4,326 | Data keys and half-open slicing; Holm/Bonferroni |
| `resample_rule_test.py`, `selftest_research_cli.py`, `selftest_all.sh` | 9,693 / 7,875 / 941 | Tests (14 self-test scripts in `selftest_all.sh`) |
| `retired/costs_scenarios_AB.py`, `retired/README_retired.md` | 4,587 / 1,864 | Archive of the Scenario A/B cost model and the retirement note |
| `requirements.txt` | 332 | Includes `statsmodels>=0.15` |
| Unmodified originals | | `config_schema.py`, `diagnose_gaps.py`, `stats_fingerprint.py`, `walkforward.py` |

The earlier delivery folders (with `.patch` files) lived in the previous assistant's output area and are not needed: the installed repo is the source of truth. `scripts/backtest.sh` (1,980 bytes) takes its `--fee` from `python3 user_data/analysis/costs.py --per-side "$COST_CASE"` (`COST_CASE` defaults to `base`; freqtrade has no slippage setting, so the per-side fee plus slippage is passed as the fee, agreeing with `costs.py net_return` within 1e-8), its `TIMERANGE` ends 20250831 (the holdout lock covers strategy backtests), and `PRINT_ONLY=1` prints the resolved command without running Docker. Nothing else in the script changed (verified with a stub `docker`: only the `--fee` and `--timerange` values differ from the previous script).

## 4. Data state (verified on real data)

| File | Span | Rows | Notes |
|---|---|---|---|
| `ETH_FDUSD-1m` (freqtrade and raw, identical) | 2026-01-30 00:00 to 2026-09-19 23:59 | 335,520 | 0 missing bars; 32,170 zero-trade minutes (9.59%); entirely post-break |
| `ETH_USDT-1m`, `FDUSD_USDT-1m` (raw) | same | 335,520 | ETH_USDT 0 zero-trade; FDUSD_USDT 15,010 (4.47%) |
| `ETH_FDUSD` 5m/15m/30m/1h/1d/1w | 2023-08-04 to 2026-09-19 | 329,088 (5m) | About 80% pre-break (2026-01-29 break) |
| `ETH_USDT-5m` (raw) | 2023-08-04 to 2026-09-19 | 329,088 | |
| `ETH_USDT-1h` / `-1d` (raw) | 2017-08-17 to 2026-09-19 | 79,572 / 3,321 | 1h has 128 missing hours (not yet located) |

- **Raw columns:** date, open, high, low, close, volume, quote_volume, n_trades, taker_buy_base, taker_buy_quote. Binance does emit zero-trade klines, so there are no literal missing minutes in ETH/FDUSD.
- **Flat means zero trades:** O=H=L=C and volume 0 is the same set as n_trades=0 (checked on real data).
- **Seam (verified):** the original 1m download started 2026-03-19; the extension was joined there. Seam check passed on all four 1m series (69,120 rows before, 266,400 after), and `task0_report.py --previous-1m ETH_FDUSD-1m.backup-20260920T002449Z.feather.bak` proved the extended file reproduces the original exactly (0 rows lost). The other two backups in the data dir are copies of the already-extended file.
- **Planning fee: 0.10% for makers and 0.10% for takers.** The account's current maker 0% is a promotion and is ignored; promotions are never modeled (work order 1.2). Cost cases: base = fee 0.10% + slippage 0.02% per side (0.24% round trip); stress = 0.12% + 0.03% per side (0.30% round trip); taker execution at the next bar's open (`costs.py`).
- **Exchange facts (Binance `exchangeInfo`):** ETHFDUSD tick 0.01, step 0.0001, min notional 5; LIMIT_MAKER supported on all three symbols.
- **ETH/FDUSD walk-forward capacity (fit 90 / validate 30):** 4 splits on 232 days; 270/90 gives 0. Reference only: the research pair is ETH/USDT (fit 365 / validate 90; section 6).
- **Pending data batch (section 7, item 3):** ETH/USDT 5m back to 2017-08-17 (the raw store currently starts 2023-08-04), BTC/USDT 1h/1d from 2017-08-17, ETHUSDT and BTCUSDT perpetual funding-rate history, the 128 missing ETH/USDT hours located, and a monthly liquidity table. None of it is on disk yet.

## 5. Results so far (HISTORY: ETH/FDUSD post-break; the maker/resting-order line is retired)

**Task 0 findings.** 1d built from 1m: 5 of 233 days differed in `open` only, all with a flat first/last minute. **Traded-minutes resampling:** ZERO mismatches on 5m (67,104 bins), 15m (22,368), 30m (11,184), 1h (5,592), 1d (233), 1w (32). The old rule mismatched native `open` on 5,111 of 5m bins (7.6%), `high` 1,377, `low` 1,578.

**Flat share is non-stationary (monthly, measured).** ETH/FDUSD zero-trade share by month, 2026: Jan 0.0%, Feb 0.4%, Mar 3.0%, Apr 8.1%, May 13.9%, Jun 6.6%, Jul 14.1%, Aug 21.3%, Sep (19 days) 8.4%. FDUSD/USDT: 0.1, 0.5, 3.0, 5.1, 4.3, 3.7, 7.1, 6.9, 5.3%. (An earlier draft called this a step at 2026-03-19; that was an artifact of splitting at the original download's start. Do not repeat it.) The 5m flat share fell from about 4.1% pre-break to 0.39% post-break.

**Equal-span fingerprint (2026-01-30 to 2026-09-19).** Excess kurtosis of returns: 1m 80.6, 5m 31.7, 15m 21.9, 30m 20.4, 1h 11.3, 1d 5.7, 1w 5.0 (n=31). Stationary everywhere. Lag-1 autocorrelation is tiny: 1m -0.011, 5m -0.0013, 15m -0.0002, 30m +0.0076; Ljung-Box significant at 1m to 30m, not at 1h, 1d, 1w. Big-move bars carry more volume on every timeframe except 1w.

**Event-anchored lead-lag (post-break, strict alignment; fine-bar units; family 15+15+36 = 66 tests).**

| Pair | Best lag | n | beta | corr | raw p | Holm (own family) | Holm (all 66) |
|---|---|---|---|---|---|---|---|
| 1m:5m | 6 bars (6 min) | 67,101 | -0.0088 | -0.018 | 0.0203 | 0.305 | 1.00 |
| 5m:15m | 5 bars (25 min) | 22,365 | -0.0151 | -0.025 | 0.0253 | 0.379 | 1.00 |
| 5m:1h | 25 bars (125 min) | 5,588 | -0.0212 | -0.067 | 0.00189 | 0.068 | 0.124 |

Nothing survives. The 1m:5m best lag moved from +10 min (beta +0.0130, 184 days) to 6 min (beta -0.0088, 232 days): sign flip, "jumping lags" (a warning per the mathematician). The `hac_best`/`nonoverlapping_best` entries are the known overlap artifact, diagnostics only.

**Task 2, rerun on the extended 233 days (about 5,590 origins per cell).** All 36 cells negative (adverse selection), 16 of 36 significant after Holm (8 per variant), means about -0.003% to -0.034% before fees; fill rate about 72% / 57% / 33% at d = 0.05% / 0.10% / 0.20%. On the earlier 184 days it was 14 of 36 significant and down to -0.055%. Variants `A_14bars` (headline) and `B_15bars` differ negligibly. Result files: `results/runs/20260920T014429Z_passive_fill_baseline.{csv,json}` (extended) and `20260919T125727Z_...` (184 days).

## 6. Decisions in force (work orders 1.2 and 1.3, plus items of work order 1.1 / answers 01-02 not superseded)

**Cost model.** Fee 0.10% per side plus slippage 0.02% per side (base, 0.24% round trip); stress 0.12% plus 0.03% per side (0.30%); taker at the next bar's open; promotions never modeled.

**Materiality and classification.** M = 0.35% gross per round trip for event-type primary cells (Task 3), measured as the excess over the unconditional mean forward return at the same horizon. Pass = one-sided p < 0.05 and excess at least M; Dead = one-sided 95% upper bound below M; otherwise inconclusive. Forward success = one-sided p < 0.05 and an estimate at least half the discovery estimate, and also at least M for event cells. Report the minimum detectable effect from the standard error.

**Sample design.** ETH/USDT is the research pair. Discovery and walk-forward use data up to and including 2025-08-31, enforced by an `--end-date` guard; later data is a locked holdout readable only by the forward-test command, once per candidate, Holm across candidates that reach it (registry). Walk-forward on ETH/USDT: fit 365 / validate 90 days; ETH/FDUSD keeps its post-break 90/30 rules, reference only. Replace the by-month table with a by-year table plus leave-one-year-out; if dropping any single year cuts the estimate by more than half or flips its sign, the candidate does not advance without the mathematician's review.

**Pass rules (unchanged unless the mathematician says otherwise).**
- Holm is the gate within each pre-registered family (Bonferroni for reference); cumulative count reported; every cell tagged `pre-registered` or `exploratory`; correlation reported beside beta.
- Lag selection in a fit window: smallest p (largest |HAC t|), no "significant" pre-filter, not |beta| or |correlation|. (The code still selects the largest |beta| among significant lags; this is queue item 1 of section 7.)
- Walk-forward is a stability check, not independent evidence. Validate at the fit-selected lag: (a) same sign in every window; (b) one-sided Stouffer of validate z (z = orientation sign x HAC t, equal weights), p < 0.05, Holm across candidates carried forward; (c) no split significantly opposite (two-sided p < 0.05, opposite sign). Report the selected lag per split; jumping lags are a warning.
- Orientation sign: the pre-registered direction for pre-registered cells (S1 positive, S2 negative, no lag selected); each split's fit-window sign for two-sided cells and lag scans (Task 3).
- Freeze: a manifest timestamp fixing the exact cell, direction, discovery estimate and dispersion, and the code commit; one forward test on the holdout, then consumed; failure means dead, no retuning.
- Resampling: traded-minutes rule (adopted; zero mismatches on the real ETH/FDUSD data).

**Confirmed Task 3 and Task 4 interpretations (kept).** Task 3: 13 bars between kept events (not 12). Task 4: hour-of-day (24 levels) instead of half-hour; the volume ratio compares the same 60-minute clock window against its median over the prior 14 days; the 5-day range uses closed daily bars only. Task 3 changes per work order 1.2: the pair is ETH/USDT under the cutoff above; report the raw mean and the excess over the unconditional mean; the three-way rule with M = 0.35% replaces the earlier triage.

**Work order 1.3 clarifications (full text in 7.6 onward; these supersede the "carried over" paragraph that used to stand here).**
- Forward-test minimum sample restored (7.6): holdout at least 45 days and at least 100 events or observations for the primary cell, SE no larger than half the claimed effect (discovery dispersion scaled to holdout size); otherwise wait, and if more than 120 further days would be needed, ask the mathematician.
- Slope cells S1 and S2: M applies to event cells only; instead an economic gate at stress cost (pooled net return > 0 and net return >= 0 in at least 70% of the 90-day validate windows; cash windows count 0), plus net-positive at stress cost at the holdout (7.7). Pre-registered rules: S1 long from the next open if x (L = 40) > 0, else cash; S2 long from the open of the 1h bar at s+1h while the latest rank <= 0.5, else cash.
- S2 endpoints in clock time; rank window 90 days (7.8). Cutoff 2025-08-31 23:59:59 UTC inclusive with an origin purge, no truncation (7.9). Partial years shown but not judged below 120 days (7.10). Task 4 defined (7.11). Registry starts empty for ETH/USDT; earlier ETH/FDUSD runs imported as `history` / `void_pre_fix`, excluded from Holm and the cumulative count (7.12). **Primary timeframe decided: 5m and above; 1m is not used in the new phase (7.13).**
- Mistake log additions: #9 (reference sign from the full sample) and #10 (raw forward returns pass on drift alone; use the excess over the unconditional mean).

## 7. New-phase order (7.1-7.5 verbatim from work order 1.2; 7.6 onward from work order 1.3 section A)

The five parts of the verbatim block below are **7.1** Materiality and classification, **7.2** Sample design (ETH/USDT is the research pair), **7.3** Data batch, **7.4** Queue, **7.5** Specs. Sections 7.6 onward follow the verbatim block.

*Mathematician, 2026-09-20.* Costs: fee 0.10% per side plus slippage 0.02% per side (base round trip 0.24%); stress 0.12% plus 0.03% per side (0.30%); taker at the next bar's open; promotions never modeled.

**1. Materiality and classification**
- M = 0.35% gross per round trip, measured as the excess over the unconditional mean forward return at the same horizon, so drift alone cannot pass. It applies to event-type primary cells (Task 3).
- Pass = one-sided p < 0.05 and excess ≥ M. Dead = one-sided 95% upper bound below M. Otherwise inconclusive.
- Forward success = one-sided p < 0.05 and an estimate at least half the discovery estimate, and also ≥ M for event cells.
- Report MDE from the standard error.

**2. Sample design (ETH/USDT is the research pair)**
- Discovery and walk-forward use data up to and including 2025-08-31, enforced by an `--end-date` guard.
- Data after that date is a locked holdout, readable only by the forward-test command, once per candidate. Holm across candidates that reach it, via the registry.
- Walk-forward on ETH/USDT: fit 365 / validate 90 days. ETH/FDUSD keeps its post-break 90/30 rules, for reference only.
- Replace the by-month table with a by-year table plus leave-one-year-out. Same red flag: if dropping any single year cuts the estimate by more than half or flips its sign, the candidate does not advance without my review.

**3. Data batch** (one owner run; copy-pasteable commands, no placeholders)
1. ETH/USDT 5m back to 2017-08-17 (resample 15m with the traded rule). BTC/USDT 1h and 1d from 2017-08-17 (replication asset).
2. ETHUSDT and BTCUSDT USD-M perpetual funding-rate history from the first available settlement: time (UTC), rate, settlement interval. If the interval changes, normalize to 8h and report where.
3. Locate the 128 missing ETH/USDT hours (list the gaps, check the same gaps in 5m). Mask them, never drop.
4. One table from the raw 1m store: monthly trades per day and quote volume, ETH/FDUSD vs ETH/USDT, 2026.

**4. Queue**
1. Selection-rule change (min-p, no pre-filter).
2. Pass-rule machinery, registry, freeze/forward-test command, `--end-date` guard and holdout lock.
3. Data batch.
4. S2, S1, then Task 3.
5. Rule evaluator and Task 4, only if a candidate passes discovery.

Each result reports family size, adjusted p, by-year table, leave-one-year-out, correlation beside beta, and (S1 and S2 only) the BTC sign, report only.

**5. Specs**

*S2 `funding_extreme`* (family 2; ETHUSDT perpetual funding, ETH/USDT spot prices)
- Predictor: funding rate at each settlement s; rank = its percentile within the previous 270 settlements (current excluded).
- Target: ETH/USDT spot log return from the open of the 1h bar starting at s+1h to +24h and +72h.
- Test: OLS slope of forward return on rank, all settlements, Newey-West HAC (3 lags for 24h, 9 for 72h). Direction negative. Primary: 72h.
- Exploratory, descriptive only: mean forward return and excess in rank ≤ 0.10 and ≥ 0.90.
- Plausible if real: correlation −0.03 to −0.08; above 0.2 means a bug.

*S1 `trend_slope`* (ETH/USDT 1d; family 10)
- Predictor at the close of day t: x = ln(C_t / C_{t−L}) / σ30, where σ30 = std of the last 30 daily log returns ending at t; L ∈ {10, 20, 40, 80, 160}.
- Target: ln(C_{t+h} / C_t), h ∈ {1, 5}; all daily origins; Newey-West HAC (5 lags for h = 1, 10 for h = 5). Direction positive.
- Primary: L = 40, h = 5. The other nine cells are exploratory (Holm within the 10).
- Gut-check: x uses closes ≤ t; y uses closes > t.

*Task 3 `shock_response`*: as in `workorder-01.md` plus the confirmed interpretations in this handoff, with these changes:
- The pair is ETH/USDT, under the cutoff above.
- Report the raw mean and the excess over the unconditional mean.
- The 0.05%/0.20% triage is replaced by the three-way rule with M = 0.35%.

*Rule evaluator* (only once a candidate passes discovery)
- Vectorized long/flat rule on closed bars; entry and exit at the next bar's open; base and stress costs as above.
- Outputs: trades, hit rate, mean gross and net per trade; per window growth per week, max drawdown, share of positive months; by-year table; buy-and-hold and cash as benchmarks.
- Self-tests on synthetic data: a random walk must be net-negative after costs, and an injected drift must be recovered.

### 7.6 Forward-test minimum sample (restored; the order above omitted it)
The holdout test runs only when, at test time, the holdout has at least 45 days and at least 100 events or observations for the primary cell. The standard error must also be no larger than half the claimed effect (from the discovery dispersion, scaled to holdout size). Otherwise wait; if it would need more than 120 further days, ask the mathematician.

### 7.7 Slope cells (S1, S2): economic gate
- M applies to event cells only.
- Between stability and freeze, run the frozen rule in the rule evaluator at stress cost over the discovery sample. Require pooled net return > 0 and net return >= 0 in at least 70% of the 90-day validate windows (a window fully in cash counts as 0).
- At the holdout, the frozen rule must also be net-positive at stress cost, in addition to the slope cell's forward success.
- Pre-registered rules:
  - S1: long from the next open if x (L = 40) > 0 at the day's close, otherwise cash.
  - S2: long from the open of the 1h bar at s+1h while the latest rank <= 0.5, otherwise cash, re-evaluated at each settlement.

### 7.8 S2 endpoints
- Entry = open of the 1h bar labeled s+1h; exit = open of the 1h bar labeled s+1h+H, H = 24h and 72h in clock time.
- The rank window is the previous 90 days of settlements (270 at 8h).
- If any interval other than 8h appears, report the dates and set HAC lags to ceil(H / shortest interval).

### 7.9 Cutoff mechanics
- The cutoff is 2025-08-31 23:59:59 UTC inclusive. Last usable bars: 1d labeled 2025-08-31; 1h labeled 23:00; 15m labeled 23:45; 5m labeled 23:55; funding settlements up to 16:00.
- Discovery keeps an origin only if its entry is at or after the sample start and its whole target window ends at or before the cutoff. Otherwise drop it; no truncation.
- Holdout origins have entry time >= 2025-09-01 00:00 UTC, and their predictors may look back into discovery data. Straddling origins belong to neither.
- Apply the same purge at every walk-forward fit/validate boundary.

### 7.10 Partial years
Keep every calendar year as a row with n and days covered. Years with fewer than 120 days are shown but not judged: they stay in the pooled estimate and are excluded from leave-one-year-out and year-by-year sign statements.

### 7.11 Task 4
ETH/USDT, same cutoff and holdout, walk-forward fit 365 / validate 90, realized variance from 5m returns. Other definitions as confirmed (section 6). Primary = volume coefficient positive. The deliverable is the forecast series and calibration table. It runs only after a candidate passes discovery.

### 7.12 Registry
Starts empty for the ETH/USDT phase. Import the earlier ETH/FDUSD runs as `history` (runs before the strict-alignment fix as `void_pre_fix`). They are excluded from Holm and from the cumulative count for ETH/USDT candidates.

### 7.13 Primary timeframe (decided)
Signal work uses 5m and above; 1m is not used in the new phase. (The descriptive liquidity table in 7.3 item 4 reads the raw 1m store; it is a table, not signal work.)

## 8. Source documents (raw GitHub URLs)

All under `https://raw.githubusercontent.com/nitinroshia/dump-sept/refs/heads/main/`: `workorder-01.md` (original tasks), `workorder-1.1.md`, `workorder-1.2.md` (housekeeping and the new-phase order), `workorder-1.3.md` (final clarifications, script and doc consistency), `answer-01.txt`, `answer-02.txt`, `terminal-01/02/03.txt`, `tree-01.txt`. Results from the latest run: `https://raw.githubusercontent.com/nitinroshia/dump-sept-02/refs/heads/main/` (`task0_report.json`, `resample_rule_test.json`, `leadlag_summary.json`, `fingerprints.json`, `extension_run.log`, `extension_dryrun.log`, `data_ranges.json`, `resample_checks.json`). The project repo copy at `sample-bot` has a stale `leadlag.py`; the live one is 38,029 bytes on the owner's machine.

## 9. Lessons from the process

- Several uploads were stale copies; this caused two rounds of confusion (the 1m extension had in fact worked at 00:24 UTC on 2026-09-20; the first backup proves it). Always check a file's internal evidence (row counts, new keys) before concluding a step failed.
- `fetch_klines.py` extends backwards by re-fetching when the existing file starts later than the job start, then merging; the freqtrade downloader did not backfill, hence `--export-freqtrade` (backup + identity checks, refuses on any mismatch).
- Mistakes fixed in shared tools: positional pairing across unequal date ranges (about 84% of 5m bars were mis-paired against the original 1m); walk-forward re-scanned all lags in validate; fit/validate boundary bar in both windows; column `lag_minutes_after_close` actually counted fine bars.

## 10. Verification

Work order 1.2 (completed on the owner's machine, `terminal-05.txt`): `ALL SELF-TESTS PASSED`; four formulas listed; `statsmodels>=0.15`; costs self-test passed.

Work order 1.3 section D (completed on the owner's machine, `terminal-06.txt`: ALL SELF-TESTS PASSED; `0.0012`; `0.0015`; `bash -n` silent; the base and stress resolved commands show `--fee 0.0012` and `--fee 0.0015` with `--timerange 20250301-20250831`):
```
cd user_data/analysis && bash selftest_all.sh 2>&1 | tail -5 && cd ../..
python3 user_data/analysis/costs.py --per-side base
python3 user_data/analysis/costs.py --per-side stress
bash -n scripts/backtest.sh
COST_CASE=base PRINT_ONLY=1 bash scripts/backtest.sh
COST_CASE=stress PRINT_ONLY=1 bash scripts/backtest.sh
```

## 11. Pending decision (owner)

The six questions of v2.1 are **answered in 7.6 onward** (forward-test minimum sample 7.6; slope-cell economic gate 7.7; S2 endpoints 7.8; cutoff mechanics 7.9; partial years 7.10; Task 4 7.11). Only one decision remains, and it belongs to the owner: **the execution pair.** Default ETH/USDT. Note that as of `tree-02.txt` the freqtrade data directory holds ETH/FDUSD only; ETH/USDT candles exist only in the raw-kline store, so a freqtrade strategy backtest on ETH/USDT needs a freqtrade download first.