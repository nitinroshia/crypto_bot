## Work order 1.3 (final)

Scope: make scripts, docs and `HANDOFF_developer.md` consistent, then re-issue the handoff as v2.2. Build nothing from the new phase.

**A. Clarifications to fold into handoff Section 7 (as 7.6 onward) and into `project_context.md`'s promotion section**
1. **Forward-test minimum sample (restored; the embedded order omitted it).** The holdout test runs only when, at test time, the holdout has at least 45 days and at least 100 events or observations for the primary cell. The standard error must also be no larger than half the claimed effect (from the discovery dispersion, scaled to holdout size). Otherwise wait; if it would need more than 120 further days, ask the mathematician.
2. **Slope cells (S1, S2): economic gate.**
   - M applies to event cells only.
   - Between stability and freeze, run the frozen rule in the rule evaluator at stress cost over the discovery sample. Require pooled net return > 0 and net return ≥ 0 in at least 70% of the 90-day validate windows (a window fully in cash counts as 0).
   - At the holdout, the frozen rule must also be net-positive at stress cost, in addition to the slope cell's forward success.
   - Pre-registered rules:
     - S1: long from the next open if x (L = 40) > 0 at the day's close, otherwise cash.
     - S2: long from the open of the 1h bar at s+1h while the latest rank ≤ 0.5, otherwise cash, re-evaluated at each settlement.
3. **S2 endpoints.**
   - Entry = open of the 1h bar labeled s+1h; exit = open of the 1h bar labeled s+1h+H, H = 24h and 72h in clock time.
   - The rank window is the previous 90 days of settlements (270 at 8h).
   - If any interval other than 8h appears, report the dates and set HAC lags to ceil(H / shortest interval).
4. **Cutoff mechanics.**
   - The cutoff is 2025-08-31 23:59:59 UTC inclusive. Last usable bars: 1d labeled 2025-08-31; 1h labeled 23:00; 15m labeled 23:45; 5m labeled 23:55; funding settlements up to 16:00.
   - Discovery keeps an origin only if its entry is at or after the sample start and its whole target window ends at or before the cutoff. Otherwise drop it; no truncation.
   - Holdout origins have entry time ≥ 2025-09-01 00:00 UTC, and their predictors may look back into discovery data. Straddling origins belong to neither.
   - Apply the same purge at every walk-forward fit/validate boundary.
5. **Partial years.** Keep every calendar year as a row with n and days covered. Years with fewer than 120 days are shown but not judged: they stay in the pooled estimate and are excluded from leave-one-year-out and year-by-year sign statements.
6. **Task 4.** ETH/USDT, same cutoff and holdout, walk-forward fit 365 / validate 90, realized variance from 5m returns. Other definitions as confirmed. Primary = volume coefficient positive. The deliverable is the forecast series and calibration table. It runs only after a candidate passes discovery.
7. **Registry.** Starts empty for the ETH/USDT phase. Import the earlier ETH/FDUSD runs as `history` (runs before the strict-alignment fix as `void_pre_fix`). They are excluded from Holm and from the cumulative count for ETH/USDT candidates.
8. **Primary timeframe (decided).** Signal work uses 5m and above; 1m is not used in the new phase.

**B. Scripts**
- **`costs.py`:** add a `--per-side {base,stress}` flag that prints the number (0.0012 or 0.0015) and exits. No flag still runs the self-test, and the self-test asserts the flag's output.
- **`scripts/backtest.sh`:**
  - Add a `COST_CASE` env var, default `base`. `FEE` comes from `python3 user_data/analysis/costs.py --per-side "$COST_CASE"`.
  - Comment: freqtrade has no slippage setting, so the per-side fee plus slippage is passed as the fee, which agrees with `costs.py net_return` within 1e−8.
  - `TIMERANGE` default ends 20250831, with a comment that the holdout lock covers strategy backtests too.
  - Add `PRINT_ONLY=1`, which prints the resolved command and exits without running Docker.
  - Change nothing else.

**C. Docs**
- **`README.md`:**
  - Rewrite the fee and slippage paragraphs to match B.
  - Label the 90/30 sentence as the ETH/FDUSD reference sample, and add one sentence for ETH/USDT (fit 365 / validate 90, cutoff 2025-08-31, locked holdout).
  - Correct the Python version in setup to the one in use (3.12).
- **`project_context.md`:**
  - Rewrite the Costs section to match B, and change "13 scripts" to 14.
  - In the strategy paragraph: research pair ETH/USDT, execution pair to be confirmed by the owner (default ETH/USDT).
  - Replace the "primary-timeframe question is still open" text with decision A8.
  - Add A1–A6 to the promotion section.
  - Add mistake #9 (walk-forward reference sign taken from the full sample, which contains the validate windows) and mistake #10 (raw forward returns in a drifting market pass a threshold on drift alone; measure the excess over the unconditional mean).
- **Handoff v2.2:**
  - Add the version line and a "v2.2" bullet under "What changed".
  - Section 2: self-tests passed on the owner's machine after work order 1.2 (`terminal-05.txt`).
  - Section 3: README installed; `backtest.sh` per B.
  - Section 6: add the A items.
  - Section 7: keep 7.1–7.5 verbatim and add 7.6 onward from A.
  - Section 8: add `workorder-1.3.md`.
  - Section 11: replace the six open questions with "answered in 7.6 onward". Keep only the owner's pending decision on the execution pair.

**D. Verify** (on the owner's machine, with `| tee`):
```
cd user_data/analysis && bash selftest_all.sh 2>&1 | tail -5 && cd ../..
python3 user_data/analysis/costs.py --per-side base
python3 user_data/analysis/costs.py --per-side stress
bash -n scripts/backtest.sh
COST_CASE=base PRINT_ONLY=1 bash scripts/backtest.sh
COST_CASE=stress PRINT_ONLY=1 bash scripts/backtest.sh
```
Expect ALL SELF-TESTS PASSED, `0.0012`, `0.0015`, and resolved commands showing `--fee 0.0012` and `--fee 0.0015` with a timerange ending 20250831.

**E. Close-out.** Report the files changed with byte sizes, the command outputs, and any doubt. Upload each changed file with a `-v2` suffix. Then stop.

**Next steps:** send the block to the old developer. When they reply, upload the changed files with `-v2` names and byte sizes. I'll check the same five files (handoff, `backtest.sh`, `costs.py`, README, project_context), which should be quick. After that, start the new developer with the v2.2 handoff plus the one-line instruction, and start the new mathematician chat with the attached v2.2 document.
