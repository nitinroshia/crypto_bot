# Work Order 1.3 (final) -- Close-out report (verified on the owner's machine)

**Date:** 2026-09-20. **Status:** complete; verification passed. **Scope done:** scripts, docs and the handoff made consistent; handoff re-issued as v2.2. Nothing from the new phase was built.

## Files changed (uploaded with a `-v2` suffix)

| File | Bytes | Change |
|---|---|---|
| `costs-v2.py` (-> `user_data/analysis/costs.py`) | 6,710 (was 4,267) | Added `--per-side {base,stress}` (prints `0.0012` / `0.0015` and exits; unknown case exits 2 with nothing on stdout). No flag still runs the self-test. The self-test now runs the flag in a subprocess and asserts its output, and asserts that freqtrade's accounting with fee = per-side fee + slippage agrees with `net_return` to within 1e-8 (six price pairs, both cases). |
| `backtest-v2.sh` (-> `scripts/backtest.sh`) | 1,980 (was 1,512) | `COST_CASE` env var (default `base`); `FEE` from `costs.py --per-side "$COST_CASE"`; comment on slippage as required; `TIMERANGE` ends 20250831 with the holdout-lock comment; `PRINT_ONLY=1` prints the resolved command and exits. Nothing else changed. |
| `README-v2.md` (-> `README.md`) | 16,620 (was 15,751) | Fee and slippage section rewritten to match; 90/30 sentence labeled as the ETH/FDUSD reference sample plus one sentence for ETH/USDT (fit 365 / validate 90, cutoff 2025-08-31, locked holdout); setup Python corrected to 3.12. |
| `project_context-v2.md` (-> `project_context.md`) | 37,284 (was 33,210) | Costs section rewritten; "13 scripts" -> 14; strategy paragraph (research pair ETH/USDT, execution pair to be confirmed by the owner, default ETH/USDT); the primary-timeframe text replaced with decision A8; A1-A6 added to the promotion section; mistakes #9 and #10 added (and the closing pattern sentence extended). |
| `HANDOFF_developer-v2.md` (v2.2) | 27,757 (was 20,386) | Version line and a v2.2 bullet; section 2 (self-tests passed after work order 1.2, `terminal-05.txt`); section 3 (README installed; `backtest.sh` per B; `costs.py` row); section 6 (A items); section 7 keeps 7.1-7.5 verbatim (checked by exact-match) and adds 7.6-7.13 from A; section 8 adds `workorder-1.3.md`; section 10 verification; section 11 now says the six questions are answered in 7.6 onward and keeps only the owner's execution-pair decision. |

## Command output (owner's machine, `terminal-06.txt`; the development sandbox produced the same)

    ALL SELF-TESTS PASSED                      (pandas 3.0.2, 14 scripts)
    0.0012                                     (costs.py --per-side base)
    0.0015                                     (costs.py --per-side stress)
    bash -n scripts/backtest.sh                (no output, exit 0)
    docker compose run --rm freqtrade backtesting --config user_data/config.json --config user_data/test_params.json --strategy MomentumTrailing --timerange 20250301-20250831 --fee 0.0012 --cache none
    docker compose run --rm freqtrade backtesting --config user_data/config.json --config user_data/test_params.json --strategy MomentumTrailing --timerange 20250301-20250831 --fee 0.0015 --cache none

On pandas 2.2.3 with statsmodels 0.14.2 every script passes except `stats_fingerprint.py` (the known statsmodels < 0.15 keyword issue). A stub-`docker` run showed the executed arguments differ from the previous live script only in the `--fee` value (0.001 -> 0.0012) and the `--timerange` end (20260901 -> 20250831); the printed command is identical to the executed one. **Owner-machine output (`terminal-06.txt`):** identical to the above. `ALL SELF-TESTS PASSED`; `0.0012`; `0.0015`; `bash -n` printed nothing; the base and stress resolved commands show `--fee 0.0012` and `--fee 0.0015` with `--timerange 20250301-20250831`. Every expected item in section D is met.

## Doubts
1. **Execution pair.** As of `tree-02.txt` the freqtrade data directory holds ETH/FDUSD only; ETH/USDT candles exist in the raw store, not in freqtrade format. `backtest.sh` runs whatever pair `config.json` names. A freqtrade backtest on ETH/USDT needs a freqtrade download first, and this is the owner's pending decision (recorded in handoff section 11).
2. **Python version.** README setup now says 3.12 as instructed. I wrote `pyenv install 3.12` (pyenv resolves the latest 3.12.x) and have not seen the owner's exact patch version.
3. **Order of the queue vs 7.7.** The economic gate for S1 and S2 needs the rule evaluator between stability and freeze, but 7.4 lists the evaluator last (item 5, "only if a candidate passes discovery"). That is consistent for discovery and walk-forward, but a slope-cell candidate cannot be frozen until the evaluator is built.
4. **"Sample start" in 7.9.** "Entry at or after the sample start" does not say whether that is the first bar of the dataset or the first origin with a full predictor lookback (S1 needs 160 days for L = 160, S2 needs 90 days of settlements). I would treat it as the first origin with a complete lookback unless told otherwise.

Verified, not doubts: the 1e-8 agreement claim holds (largest measured difference 1.1e-9), so `net_return` was not changed; `PRINT_ONLY` uses a command array so the printed and executed commands cannot drift apart.

Stopping here as instructed.